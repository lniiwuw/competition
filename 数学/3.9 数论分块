3.9 数论分块

一维数论分块
int init(){
  int ans = 0;
    for(int l = 1, r; l <= n; l = r + 1){
        r = n/(n/l);
        ans += (n/l) * (r - l + 1); 
    }
    return ans;
}

二维数论分块
int init(){
    int ans = 0;
    for(int l = 1, r; l <= min(n, m); l = r + 1){
        r = min(n/(n/l), m/(m/l));
        ans += (n/l) * (m/l) * (r - l + 1); 
    }
    return ans;
}
